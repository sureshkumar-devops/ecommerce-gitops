apiVersion: v1
kind: Secret
metadata:
  name: argocd-vault-credentials
  namespace: argocd
type: Opaque
stringData:
  VAULT_ADDR: "http://vault.vault.svc.cluster.local:8200"
  VAULT_TOKEN: "PLACEHOLDER_TOKEN_REPLACE_AFTER_INIT"
  VAULT_SKIP_VERIFY: "true"  # Set to true for development (no TLS)
