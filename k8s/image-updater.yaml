apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: ecommerce-image-updater
  namespace: argocd
  labels:
    app.kubernetes.io/managed-by: helm
    app.kubernetes.io/part-of: ecommerce
    environment: gitops
spec:
  # This namespace field tells the updater where to look for the Applications
  namespace: argocd
  applicationRefs:  
    #------------ DEVELOPMENT --------------------
    - namePattern: "dev-svc-my-app"
      images:
        - alias: "app"
          imageName: "lehardocker/ecommerce-app"
          commonUpdateSettings:
            updateStrategy: "newest-build"
            allowTags: "regexp:^dev-.*"
          manifestTargets:
            helm:
              name: "image.repository"
              tag: "image.tag"
      writeBackConfig:
        method: "git:secret:argocd/git-creds"
        gitConfig:
          repository: "https://github.com/sureshkumar-devops/ecommerce-gitops.git"
          branch: "dev"
          writeBackTarget: "environments/dev/values.yaml"
          #writeBackTarget: "helmvalues:environments/dev/values.yaml"
          #commitMessage: "[ImageUpdater] {{app}} → {{image}}:{{newTag}} (env=dev)"

   # --- STAGING ---
    - namePattern: "staging-svc-my-app"
      images:
        - alias: "app"
          imageName: "lehardocker/ecommerce-app"
          commonUpdateSettings:
            updateStrategy: "newest-build"
            allowTags: "regexp:^stag-.*"
          manifestTargets:
            helm:
              name: "image.repository"
              tag: "image.tag"
      writeBackConfig:
        method: "git:secret:argocd/git-creds"
        gitConfig:
          repository: "https://github.com/sureshkumar-devops/ecommerce-gitops.git"
          branch: "staging"
          writeBackTarget: "environments/staging/values.yaml"

    # --- PRODUCTION ---
    - namePattern: "prod-svc-my-app"
      images:
        - alias: "app"
          imageName: "lehardocker/ecommerce-app"
          commonUpdateSettings:
            updateStrategy: "newest-build"
            allowTags: "regexp:^v[0-9]+\\.[0-9]+\\.[0-9]+-.*"
          manifestTargets:
            helm:
              name: "image.repository"
              tag: "image.tag"
      writeBackConfig:
        method: "git:secret:argocd/git-creds"
        gitConfig:
          repository: "https://github.com/sureshkumar-devops/ecommerce-gitops.git"
          branch: "master"
          writeBackTarget: "environments/prod/values.yaml"

